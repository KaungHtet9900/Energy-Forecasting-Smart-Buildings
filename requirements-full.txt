# Full research environment (data pipeline + models + export).
# The slim root requirements.txt is for the deployed Streamlit dashboard only.

# Core scientific stack
numpy>=1.26
pandas>=2.1
scipy>=1.11
pyarrow>=14.0          # parquet I/O

# ML / DL
scikit-learn>=1.3
torch>=2.1             # CPU build is fine; code is device-agnostic
lightgbm>=4.0
xgboost>=2.0
statsmodels>=0.14

# Visualization
matplotlib>=3.8
seaborn>=0.13

# Data acquisition
requests>=2.31
kaggle>=1.6           # GEPIII download (Kaggle dataset mirror)

# Utilities
PyYAML>=6.0
tqdm>=4.66
holidays>=0.40        # calendar holiday features
tabulate>=0.9         # markdown/LaTeX tables

# Document export
reportlab>=4.0        # manuscript -> PDF
python-docx>=1.1      # manuscript -> DOCX

# Dashboard
streamlit>=1.30
